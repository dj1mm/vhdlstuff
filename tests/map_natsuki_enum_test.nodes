
#include <catch2/catch.hpp>
#include <unordered_map>
#include <string>

#define string std::string

[[namespace=map_natsuki_enum_test]];
[[visitable]];

enum e {A,B,C,D,E,F};
class a(m_e: e<string>);

public:
{

TEST_CASE("map enum", "[map_natsuki_enum_test]")
{
    auto a_ = std::make_unique<a>();
    REQUIRE(a_->m_e.size() == 0);
}

}
